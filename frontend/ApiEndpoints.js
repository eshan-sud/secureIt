// frontend/src/ApiEndpoints.js

// Base URL
const baseURL = "http://localhost:8000/api/v1/";

export const endpoints = {
  // Data Endpoints
  uploadData: `${baseURL}data/uploadData`, // POST
  // readData: `${baseURL}data/readData`, // GET
  // updateData: `${baseURL}data/updateData`, // PATCH
  // deleteData: `${baseURL}data/deleteData`, // DEL
  // downloadData: `${baseURL}data/downloadData`, // GET

  // Permission Endpoints
  // readPermissions: `${baseURL}permissions/readPermissions`, // GET
  // updatePermissions: `${baseURL}permissions/updatePermissions`, // PATCH
  // removePermissions: `${baseURL}permissions/removePermissions`, // DEL

  // 3rd Party
  // login: // POST
  // requestData: // GET
  // 
};
